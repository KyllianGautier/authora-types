/** Input for setting up multi-factor authentication. */
export interface SetupMultiFactorAuthInput {
  /** The user's email address. */
  email: string;
  /** The user's password. */
  password: string;
}
